{
  "name": "osci-migration",
  "version": "0.5.0",
  "description": "JSON-producing DOM query scripts for OSCI HTML",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL",
  "dependencies": {
    "jsdom": "^24.0.0"
  }
}
